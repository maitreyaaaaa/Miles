{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd Application/frontend && npm install && npm run build",
  "outputDirectory": "Application/frontend/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
